// Ported from the frontend's original riskEngine.js — owner: Jeswin
// Keep this as the single source of truth for risk scoring; the frontend
// should NOT independently recompute risk once this is wired in.

export function computeRisk(params) {
  throw new Error('Port the logic from the original riskEngine.js here');
}
