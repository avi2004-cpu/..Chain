# identity chaincode

Deploy/test locally via the Fabric CLI before wiring up the backend:
```
peer chaincode invoke ...
peer chaincode query ...
```
See fabric-samples/test-network docs for exact deploy commands.
